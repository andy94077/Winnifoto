{
  "name": "winnifoto",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start-frontend": "cd frontend && yarn start",
    "heroku-prebuild": "cd backend && yarn",
    "build": "cd backend && yarn seed",
    "build-frontend": "cd frontend && yarn build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}